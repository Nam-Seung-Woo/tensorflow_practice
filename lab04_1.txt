멀티 베리어블(multi-variable)
하이파서시스에서 x와 w의 값이 여러개인 식

매트릭스 곱셈
두 배열의 행렬을 곱해? 값을 도출하는 방법.
매트릭스는 대문자로 쓴다.

               w1
(x1 x2 x3) * ( w2 ) =  (x1w1+x2w2+x3w3)
               w3

    X           W

Hypothesis(X)=XW(매트릭스의 곱)

저 식에서, X에 들어가는 값들을 인스턴스(instance)라고 한다.